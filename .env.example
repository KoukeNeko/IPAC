# 環境變數範例檔案
# 複製此檔案為 .env 並根據你的環境設定

# Django 設定
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# 資料庫設定
# 開發環境使用 SQLite
USE_SQLITE=True

# 生產環境使用 PostgreSQL（註解掉 USE_SQLITE）
# DB_NAME=icap_db
# DB_USER=icap_user
# DB_PASSWORD=icap_password
# DB_HOST=localhost
# DB_PORT=5432

# 時區設定
TIME_ZONE=Asia/Taipei

# 靜態檔案設定
STATIC_URL=/static/
STATIC_ROOT=/var/www/icap/static/

# 媒體檔案設定
MEDIA_URL=/media/
MEDIA_ROOT=/var/www/icap/media/

# CORS 設定（如果需要）
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Email 設定（用於密碼重設等功能）
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-email-password
